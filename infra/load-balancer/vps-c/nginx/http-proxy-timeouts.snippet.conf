# Вставить внутрь блока http { ... } файла /etc/nginx/nginx.conf
# либо подключить через include (если ваш шаблон nginx это поддерживает)

    proxy_connect_timeout 5s;
    proxy_send_timeout    60s;
    proxy_read_timeout    60s;
    send_timeout          60s;
