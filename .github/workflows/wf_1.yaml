name: CI/CD pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# ВАЖНО: Разрешаем Actions записывать собранные образы в GitHub Packages (GHCR)
permissions:
  contents: read
  packages: write

jobs:
  # 1. Unit Тесты и анализ покрытия кода (БЕЗ ИЗМЕНЕНИЙ)
  test_and_coverage:
    name: Tests & Coverage
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Start required isolated services for tests
        run: |
          echo "POSTGRES_USER=postgres" >> .env
          echo "POSTGRES_PASSWORD=postgres" >> .env
          echo "POSTGRES_DB=postgres" >> .env
          echo "ORDER_DB_USER=order_admin" >> .env
          echo "ORDER_DB_PASSWORD=order_pass" >> .env
          echo "INV_DB_USER=inv_admin" >> .env
          echo "INV_DB_PASSWORD=inv_pass" >> .env
          echo "RMQ_USER=rmquser" >> .env
          echo "RMQ_PASSWORD=rmqpass" >> .env
          echo "MINIO_ROOT_USER=miniouser" >> .env
          echo "MINIO_ROOT_PASSWORD=miniopass" >> .env
          echo "ORDER_PORT=8001" >> .env
          echo "INVENTORY_PORT=8002" >> .env

          docker compose up -d postgres rabbitmq minio

          echo "Waiting for PostgreSQL to start..."
          until docker compose exec -T postgres pg_isready -U postgres -d postgres; do
            sleep 2
          done

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-cov asyncio httpx
          for req in $(find services -name "requirements*.txt"); do pip install -r $req; done

      - name: Run Pytest over tests
        run: |
          export PYTHONPATH="$(pwd):$(pwd)/services/inventory-service"
          pytest services/tests/test_inventory_logic.py --cov=services/inventory-service --cov-report=term-missing --cov-report=xml --cov-append

          export PYTHONPATH="$(pwd):$(pwd)/services/order-service"
          pytest services/tests/test_order_logic.py --cov=services/order-service --cov-report=term-missing --cov-report=xml --cov-append

          export PYTHONPATH="$(pwd):$(pwd)/services/slicer-adapter"
          pytest services/slicer-adapter/tests/ --cov=services/slicer-adapter --cov-report=term-missing --cov-report=xml --cov-append
        continue-on-error: true

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}

  # 2. SAST на весь handwritten код проекта (БЕЗ ИЗМЕНЕНИЙ)
  security_sast:
    name: SAST
    needs: test_and_coverage
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Bandit
        run: pip install bandit

      - name: Run Bandit on handwritten services
        run: bandit -r services/order-service services/inventory-service services/slicer-adapter -x "*/tests/*","*test_*\.py"
        continue-on-error: true

  # 3. Build + SCA + Push в GHCR (АДАПТИРОВАНО ПОД КЛАСТЕР)
  build_i_sca:
    name: Build, SCA and Push
    runs-on: ubuntu-latest
    needs: security_sast
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Авторизуемся в GitHub Container Registry
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Docker требует имя репозитория в нижнем регистре
      - name: Set lower case owner name
        run: echo "REPO_LC=${GITHUB_REPOSITORY,,}" >> ${GITHUB_ENV}

      # --- СБОРКА И ПРОВЕРКА ORDER SERVICE ---
      - name: Build Order Service image
        # ВАЖНО: Указываем --target production для многоэтапной сборки
        run: docker build --target production -t ghcr.io/${{ env.REPO_LC }}/order-service:latest ./services/order-service

      - name: Scan Order Service image with Trivy
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ghcr.io/${{ env.REPO_LC }}/order-service:latest
          format: table
          ignore-unfixed: true
          severity: 'HIGH,CRITICAL'
          scanners: 'vuln,secret'
          exit-code: '0'

      - name: Push Order Service image
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: docker push ghcr.io/${{ env.REPO_LC }}/order-service:latest

      # --- СБОРКА И ПРОВЕРКА INVENTORY SERVICE ---
      - name: Build Inventory Service image
        run: docker build --target production -t ghcr.io/${{ env.REPO_LC }}/inventory-service:latest ./services/inventory-service

      - name: Scan Inventory Service image with Trivy
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ghcr.io/${{ env.REPO_LC }}/inventory-service:latest
          format: table
          ignore-unfixed: true
          severity: 'HIGH,CRITICAL'
          scanners: 'vuln,secret'
          exit-code: '0'

      - name: Push Inventory Service image
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: docker push ghcr.io/${{ env.REPO_LC }}/inventory-service:latest

      # --- СБОРКА И ПРОВЕРКА SLICER ADAPTER ---
      - name: Build Slicer Adapter image
        run: docker build --target production -t ghcr.io/${{ env.REPO_LC }}/slicer-adapter:latest ./services/slicer-adapter

      - name: Scan Slicer Adapter image with Trivy
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ghcr.io/${{ env.REPO_LC }}/slicer-adapter:latest
          format: table
          ignore-unfixed: true
          severity: 'HIGH,CRITICAL'
          scanners: 'vuln,secret'
          exit-code: '0'

      - name: Push Slicer Adapter image
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: docker push ghcr.io/${{ env.REPO_LC }}/slicer-adapter:latest

  # 4. Deploy step: на домашнем сервере (АДАПТИРОВАНО ПОД SWARM)
  deploy:
    name: Deploy to Swarm Cluster
    runs-on: self-hosted
    # needs: build_i_sca
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Раннер логинится в GHCR от лица сервера, чтобы иметь права скачать образы
      - name: Log in to the Container registry on server
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Decrypt secrets
        env:
          SOPS_AGE_KEY: ${{ secrets.SOPS_AGE_KEY }}
        run: |
          sops --decrypt --input-type dotenv --output-type dotenv infra/sops/secrets.enc.env > .env

      - name: Deploy to Docker Swarm
        run: |
          # ВАЖНО: --with-registry-auth передает токен доступа ВСЕМ воркерам в кластере (Beget, proxmox), чтобы они тоже могли скачать образы из GHCR
          docker stack deploy --with-registry-auth -c docker-stack.yml restful-slice
